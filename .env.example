# Whisper model size: tiny | base | small | medium
# small is recommended: good accuracy, runs on CPU in reasonable time
WHISPER_MODEL=small

# Set to true to save the recorded audio as a .wav file alongside results
# Useful for testing and debugging. Keep false for normal use (privacy).
SAVE_AUDIO=false

# Set to true to save a plain-text .txt transcript alongside the results JSON
# Useful for reviewing / labelling. Keep false for normal use.
SAVE_TRANSCRIPT_TXT=false

# Set to false for privacy mode: transcribed text is used for classification
# but is NOT written anywhere — only timestamps, labels, and confidence are saved.
# The plain-text transcript is also suppressed when this is false.
SAVE_TEXT_IN_JSON=true

# Classifier backend: 'ollama' (recommended) or 'regex' (fast fallback)
CLASSIFIER=ollama
# Local LLM model for classification (pull with: ollama pull gemma3:1b)
OLLAMA_MODEL=gemma3:1b

# Chunked streaming settings
# CHUNK_INTERVAL: how many seconds of audio to accumulate before transcribing and classifying.
# Lower values give more frequent live output but higher CPU load. Default: 30
CHUNK_INTERVAL=30
# CHUNK_OVERLAP: seconds of audio repeated at the start of each chunk to avoid
# cutting words at boundaries. Default: 3
CHUNK_OVERLAP=3

# Seconds of extra recording after pressing ENTER to stop.
# Prevents the last words from being cut off. Set to 0 to stop immediately.
STOP_DELAY=3

# Optional: URL of a server-time API to synchronize timestamps with a remote clock.
# When set, the speech analyzer fetches the server time once at startup, computes an
# offset from the local clock, and applies that offset to all recorded timestamps.
# Leave blank (or omit) to use the local system clock as usual.
# Expected response format: {"serverTime":{"date":"YYYY-MM-DD HH:MM:SS.ffffff","timezone_type":3,"timezone":"UTC"}}
TIME_SYNC_URL=
